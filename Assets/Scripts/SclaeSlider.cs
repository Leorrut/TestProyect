using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SclaeSlider : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
       // LeanTween.scale(gameObject, new Vector3(7f, 7f,7f), 1f).setDelay(.2f).setEase(LeanTweenType.easeInOutElastic);
    }


}
